/** @type {import('next-sitemap').IConfig} */

module.exports = {
    siteUrl: process.env.SITE_URL || 'https://neilgames.netlify.app',
    generateRobotsTxt: true, // (optional)
     // Default transformation function
  }